# Assets Directory

This directory contains static assets for the Learn C application.

## Icons
Most icons in the application are inline SVGs to ensure fast loading and easy coloring via CSS `currentColor`.

## Textures
Background textures are also handled via CSS-encoded SVGs.

## User content
If you wish to add custom images for potential future features (like diagram questions), place them here.
